{
  "name": "learn-solidity",
  "scripts": {
    "contract:compile": "hardhat compile",
    "contract:deploy": "hardhat run scripts/deploy.ts --network goerli"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.12.5"
  }
}
